<script type="text/javascript">
    $(document).ready(function () {
        scAjax({
            "url": "questions/GetPopularQuestions?count=10",
            "success": function (result) {
                if (!result) {
                    return;
                }
                $('.popularquestioncontainerdiv').show();

                $.each(result, function (key, value) {
                    if (value.answerCount > 0)
                        $('#popularquestioncloud').append("<div id='popularquestiondiv'><a class='popularquestiontitle' href='/feed/" + value.questionId + "'> " + value.title + "..</a></div>");
                    else
                        $('#popularquestioncloud').append("<div id='popularquestiondiv'><a class='popularquestiontitle' href='/feed/" + value.questionId + "'> " + value.title + "..</a></div>");
                });
            }
        });
    });
</script>
<style type="text/css">
    .popularquestioncontainerdiv {
        /*padding-top: 3px;*/
        background-color: #90A4AE;
        border: 1px solid transparent;
        border-radius: 4px;
        margin-bottom: 20px;
        border: 1px solid transparent;
    }

    .sidebar-header {
        margin-bottom: 20px;
        position: relative;
        top: 5px;
        color: #eee;
        padding-left: 10px;
    }

    #popularquestiondiv {
        padding: 4px;
    }

    .popularquestiontitle {
        font-size: 13px;
    }
    #popularquestioncloud
    {
        background: #fff;
    }

    /*question count font size in each tag*/
    #answercount {
        font-size: 10px;
    }
</style>
<div class="popularquestioncontainerdiv">
    <div class="sidebar-header">
        <h5>Popular Questions</h5>
        @*<hr id="bigseparator" />*@
    </div>
    <div id="popularquestioncloud">
    </div>
</div>
