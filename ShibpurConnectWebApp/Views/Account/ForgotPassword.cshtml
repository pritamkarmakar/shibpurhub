@model ShibpurConnectWebApp.Models.ForgotPasswordViewModel
@{
    ViewBag.Title = "Forgot your password?";
}

<script type="text/javascript">
    $(document).ready(function () {
        // disable jquery validation
        $('.forgotpassword').validate();
        $(".forgotpassword").removeAttr("novalidate");
    });
</script>


@*@using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <hr />
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="form-group">
       @Html.LabelFor(m => m.Email, new { @class = "col-md-2 control-label" })
        <div class="col-md-6">
            @Html.TextBoxFor(m => m.Email, new { @class = "form-control" , @placeholder="Enter your email"})
        </div>
    </div>
    <div class="form-group">
        <div class="col-md-10">
            <input type="submit" class="btn btn-default" value="Email Link" />
        </div>
    </div>
}*@

<div class="container" style="margin-top:30px">
    @Html.ValidationSummary("", new { @class = "text-danger" })
    <div class="col-md-4 col-md-offset-4">
        <div class="panel panel-default">
            <div class="panel-body">
                @using (Html.BeginForm("ForgotPassword", "Account", FormMethod.Post, new { @class = "forgotpassword" }))
                {
                    <span style="font-size:18px;">@ViewBag.Title</span><hr style="margin-top:10px;margin-bottom:10px;" />
                    @Html.AntiForgeryToken()
                    <div class="form-group">
                        <label for="exampleInputEmail1" class="sr-only">Email address</label>
                        <div class="input-group">
                            <span class="input-group-addon"><i class="fa fa-user fa-fw"></i></span>
                            @Html.TextBoxFor(m => m.Email, new { @class = "form-control email", @placeholder = "Enter email", @type = "email", id = "exampleInputEmail1", @required = "required" })
                        </div>
                    </div>                 
                    <button type="submit" class="btn btn-success btn-default btn-login">Email Link</button>
                }
            </div>
        </div>
    </div>
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
